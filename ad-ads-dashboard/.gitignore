# ------------------------------------------------------------
# Secrets — ห้ามส่งขึ้น build context
# ------------------------------------------------------------
.env
.env.*

# ------------------------------------------------------------
# Node/Package junk & logs
# ------------------------------------------------------------
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ------------------------------------------------------------
# VCS
# ------------------------------------------------------------
.git
.gitignore

# ------------------------------------------------------------
# Next.js build artifacts (ตัวจริงจะถูก build ใน container)
# ------------------------------------------------------------
.next
out
build
dist

# ------------------------------------------------------------
# Docker helper files (เอา compose ออกได้ ไม่จำเป็นใน build)
# ------------------------------------------------------------
docker-compose.yml
# (ปล่อยให้ Dockerfile ไม่ถูก ignore เพื่อเลี่ยงความสับสน)

# ------------------------------------------------------------
# Misc
# ------------------------------------------------------------
.DS_Store
*.pem

# ------------------------------------------------------------
# Prisma
# ------------------------------------------------------------
# **อย่า ignore โฟลเดอร์ prisma/**
# ต้องคงไว้เพื่อให้ schema/migrations ถูก copy เข้า image
# และคำสั่ง npx prisma migrate deploy ทำงานได้
#
# (ไม่ต้องใส่อะไรเพิ่ม ตรงนี้จงใจปล่อยว่าง)
